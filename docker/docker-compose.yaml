name: "garanti"

services:
  garanti:
    build:
      context: .
    volumes:
      - ../:/home/clingo/project
    hostname: PD
    deploy:
      resources:
        limits:
          cpus: 4
          memory: 2024M
